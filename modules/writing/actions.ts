"use server";
import { revalidatePath } from "next/cache"; import { z } from "zod"; import { auth } from "@/auth"; import { db } from "@/lib/db"; import { routedStructured } from "@/modules/ai/gateway"; import { requireProfileAccess } from "@/modules/learner/access";
const feedbackSchema = z.object({ grammarScore: z.number().min(0).max(100), vocabularyScore: z.number().min(0).max(100), structureScore: z.number().min(0).max(100), suggestions: z.string().min(1), rewrittenVersion: z.string().optional() });
function fallbackFeedback(content: string) { const wordCount = content.trim().split(/\s+/).filter(Boolean).length; const base = Math.min(85, 45 + wordCount); return { grammarScore: base, vocabularyScore: Math.min(90, base + 4), structureScore: Math.min(88, base + 2), suggestions: "AI 写作批改暂时未配置。请检查句首大写、句末标点，并尝试使用连接词扩展内容。", rewrittenVersion: undefined as string | undefined }; }
export async function submitWriting(formData: FormData) { const session = await auth(); if (!session?.user.id) throw new Error("UNAUTHENTICATED"); const input = z.object({ profileId: z.string().uuid(), assignmentId: z.string().uuid(), content: z.string().trim().min(20).max(10000) }).parse(Object.fromEntries(formData)); const profile = await requireProfileAccess(session.user.id, input.profileId); const assignment = await db.writingAssignment.findUniqueOrThrow({ where: { id: input.assignmentId } }); const previous = await db.writingSubmission.findFirst({ where: { assignmentId: assignment.id, learnerProfileId: input.profileId }, orderBy: { version: "desc" } }); let feedback; try { feedback = await routedStructured("WRITING", { schema: feedbackSchema, schemaName: "WritingFeedback", messages: [{ role: "system", content: `You are an English writing teacher. Grade 0-100 and provide concise Chinese suggestions. Learner level: ${assignment.level}.` }, { role: "user", content: `Assignment: ${assignment.prompt}\nStudent writing:\n${input.content}` }] }, session.user.id); } catch { feedback = fallbackFeedback(input.content); } const overallScore = (feedback.grammarScore + feedback.vocabularyScore + feedback.structureScore) / 3; await db.$transaction(async (tx) => { const submission = await tx.writingSubmission.create({ data: { assignmentId: assignment.id, learnerProfileId: input.profileId, version: (previous?.version ?? 0) + 1, parentSubmissionId: previous?.id, content: input.content, status: "REVIEWED" } }); await tx.writingFeedback.create({ data: { submissionId: submission.id, ...feedback, overallScore } }); await tx.learningActivity.create({ data: { learnerProfileId: input.profileId, familyId: profile.familyId, activityType: "WRITING_SUBMIT", module: "WRITING", score: overallScore / 100, sourceType: "WritingSubmission", sourceId: submission.id } }); }); revalidatePath("/learn/writing"); }
