export function updateStreak(currentDays: number, longestDays: number, lastStudyDate: Date | null, studyDate: Date) { const today = Date.UTC(studyDate.getUTCFullYear(), studyDate.getUTCMonth(), studyDate.getUTCDate()); const last = lastStudyDate ? Date.UTC(lastStudyDate.getUTCFullYear(), lastStudyDate.getUTCMonth(), lastStudyDate.getUTCDate()) : null; const diff = last == null ? null : Math.round((today - last) / 86400000); const current = diff === 0 ? currentDays : diff === 1 ? currentDays + 1 : 1; return { currentDays: current, longestDays: Math.max(longestDays, current), changed: diff !== 0 }; }
