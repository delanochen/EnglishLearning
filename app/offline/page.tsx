import Link from "next/link";
import { cookies } from "next/headers";
export default async function OfflinePage() {const english=(await cookies()).get("ui_locale")?.value==="en";const t=english?{title:"Cannot connect to the family NAS",help:"Confirm that this device is connected to the home network, then check that the NAS and HomeLingua containers are running. Learning data is not written to the offline cache.",retry:"Reconnect"}:{title:"当前无法连接家庭 NAS",help:"请确认设备连接到家庭网络，或者检查 NAS 和 HomeLingua 容器是否正在运行。学习数据不会写入离线缓存。",retry:"重新连接"};return <main className="grid min-h-screen place-items-center p-6"><section className="card max-w-lg text-center"><p className="text-sm font-bold uppercase tracking-[.2em] text-brand">Offline</p><h1 className="mt-3 text-3xl font-black">{t.title}</h1><p className="mt-4 text-muted">{t.help}</p><Link className="button-primary mt-6 inline-block" href="/dashboard">{t.retry}</Link></section></main>; }
