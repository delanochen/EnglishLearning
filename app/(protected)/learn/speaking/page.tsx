import { auth } from "@/auth"; import { db } from "@/lib/db"; import { ProfilePicker } from "@/components/profile-picker"; import { SpeakingRecorder } from "@/components/speaking-recorder"; import { getAccessibleProfiles } from "@/modules/learner/access";
const prompts = ["Could I have a glass of water, please?", "I would like to make an appointment for tomorrow.", "Excuse me, could you tell me where the nearest grocery store is?"];
export default async function SpeakingPage({ searchParams }: { searchParams: Promise<{ profile?: string; prompt?: string }> }) { const session = await auth(); const query = await searchParams; const profiles = await getAccessibleProfiles(session!.user.id); const selected = profiles.find((p) => p.id === query.profile) ?? profiles[0]; const prompt = prompts.includes(query.prompt ?? "") ? query.prompt! : prompts[0]; const history = selected ? await db.speakingSession.findMany({ where: { learnerProfileId: selected.id }, include: { attempts: true }, orderBy: { startedAt: "desc" }, take: 10 }) : []; return <div className="mx-auto max-w-5xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-brand">Speaking</p><h1 className="mt-2 text-4xl font-black">口语跟读</h1><ProfilePicker profiles={profiles} selectedId={selected?.id} pathname="/learn/speaking"/>{selected && <><section className="card mt-7"><p className="label">本次练习</p><h2 className="mt-2 text-2xl font-black">{prompt}</h2><div className="mt-5"><SpeakingRecorder profileId={selected.id} prompt={prompt}/></div></section><section className="mt-7"><h2 className="text-2xl font-black">练习历史</h2><div className="mt-4 space-y-3">{history.map((item) => { const attempt = item.attempts[0]; return <article className="card" key={item.id}><p className="font-bold">{item.prompt}</p><p className="mt-2 text-muted">你说：{attempt?.transcript}</p><p className="mt-2">准确度 {Math.round((attempt?.accuracyScore ?? 0) * 100)}% · 流利度 {Math.round((attempt?.fluencyScore ?? 0) * 100)}%</p><p className="mt-2 text-sm text-brand">{attempt?.feedback}</p></article>; })}</div></section></>}</div>; }
