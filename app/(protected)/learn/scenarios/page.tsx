import Link from "next/link"; import { auth } from "@/auth"; import { db } from "@/lib/db"; import { ProfilePicker } from "@/components/profile-picker"; import { getAccessibleProfiles } from "@/modules/learner/access";
export default async function ScenariosPage({ searchParams }: { searchParams: Promise<{ profile?: string; category?: string }> }) { const session = await auth(); const query = await searchParams; const profiles = await getAccessibleProfiles(session!.user.id); const selected = profiles.find((profile) => profile.id === query.profile) ?? profiles[0]; const lessons = await db.scenarioLesson.findMany({ where: { status: "PUBLISHED", ...(query.category ? { category: query.category } : {}), ...(selected?.level ? { level: selected.level } : {}) }, include: { dialogues: true, progress: selected ? { where: { learnerProfileId: selected.id } } : false }, orderBy: [{ category: "asc" }, { title: "asc" }] }); const categories = await db.scenarioLesson.findMany({ where: { status: "PUBLISHED" }, distinct: ["category"], select: { category: true } }); return <div className="mx-auto max-w-6xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-brand">US Life English</p><h1 className="mt-2 text-4xl font-black">美国生活场景课</h1><p className="mt-2 text-muted">学习自然表达、文化习惯和常见误解。</p><ProfilePicker profiles={profiles} selectedId={selected?.id} pathname="/learn/scenarios"/><div className="mt-5 flex flex-wrap gap-2"><Link className={!query.category ? "button-primary" : "button-ghost"} href={`/learn/scenarios?profile=${selected?.id ?? ""}`}>全部</Link>{categories.map(({ category }) => <Link className={query.category === category ? "button-primary" : "button-ghost"} href={`/learn/scenarios?profile=${selected?.id ?? ""}&category=${encodeURIComponent(category)}`} key={category}>{category}</Link>)}</div><div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{selected && lessons.map((lesson) => <Link className="card hover:border-brand" href={`/learn/scenarios/${lesson.id}?profile=${selected.id}`} key={lesson.id}><div className="flex justify-between"><span className="font-bold text-brand">{lesson.category} · {lesson.level}</span><span className="text-xs text-muted">{lesson.sourceType === "ORIGINAL" ? "原创" : "AI 生成"}</span></div><h2 className="mt-3 text-2xl font-black">{lesson.title}</h2><p className="mt-3 line-clamp-3 text-muted">{lesson.intro}</p><p className="mt-5 font-bold">{lesson.dialogues.length} 句对话 · {lesson.progress[0]?.completedAt ? `已完成 ${Math.round((lesson.progress[0].score ?? 0) * 100)} 分` : "开始课程 →"}</p></Link>)}</div>{selected && !lessons.length && <div className="card mt-7">当前级别还没有场景课程。</div>}</div>; }
