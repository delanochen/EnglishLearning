import Link from"next/link";import{ReadingGenerationForm}from"@/components/reading-generation-form";import{requireSystemAdmin}from"@/modules/authorization/require-admin";
export default async function GenerateReadingPage(){await requireSystemAdmin();return <div className="mx-auto max-w-4xl"><Link className="text-brand" href="/admin/content">← 返回内容管理</Link><p className="mt-5 font-bold text-brand">Structured AI generation</p><h1 className="mt-2 text-4xl font-black">AI 分级阅读生成</h1><p className="mt-2 text-muted">输出会经过 Zod 校验；失败自动重试一次，只有有效内容才写入数据库，并始终先保存为草稿。</p><ReadingGenerationForm/></div>}
