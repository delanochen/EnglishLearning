import { db } from "@/lib/db"; import { requireSystemAdmin } from "@/modules/authorization/require-admin";
export default async function AdminLogsPage(){await requireSystemAdmin();const [audits,ai,logins]=await Promise.all([db.auditLog.findMany({take:100,orderBy:{createdAt:"desc"},include:{actor:true}}),db.aIRequestLog.findMany({take:100,orderBy:{createdAt:"desc"},include:{model:true}}),db.loginAttempt.findMany({take:100,orderBy:{createdAt:"desc"}})]);return <div className="mx-auto max-w-6xl"><h1 className="text-4xl font-black">系统日志</h1><div className="mt-6 grid gap-5 xl:grid-cols-3">{[["审计日志",audits.map(x=>`${x.action} · ${x.resourceType} · ${x.actor?.email||"系统"}`)],["AI 请求",ai.map(x=>`${x.status} · ${x.purpose} · ${x.model.name} · ${x.latencyMs??0}ms`)],["登录安全",logins.map(x=>`${x.successful?"成功":"失败"} · ${x.emailHash.slice(0,12)}…`)]].map(([title,lines])=><section className="card" key={String(title)}><h2 className="text-xl font-black">{String(title)}</h2><div className="mt-3 max-h-[36rem] space-y-2 overflow-auto">{(lines as string[]).map((line,i)=><p className="rounded-lg bg-slate-100 p-2 text-xs dark:bg-slate-800" key={i}>{line}</p>)}</div></section>)}</div></div>}
