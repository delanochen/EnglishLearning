import{db}from"@/lib/db";import{requireSystemAdmin}from"@/modules/authorization/require-admin";import{formatBytes}from"@/modules/operations/status";import{archiveUploadedFile}from"@/modules/admin/actions";
export default async function AdminFilesPage(){await requireSystemAdmin();const files=await db.uploadedFile.findMany({take:200,orderBy:{createdAt:"desc"},include:{owner:true,family:true}});return <div className="mx-auto max-w-6xl"><h1 className="text-4xl font-black">上传文件</h1><p className="mt-2 text-muted">归档后文件立即停止下载，但物理文件保留到管理员完成备份和清理。</p><div className="card mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th className="p-3">文件</th><th>用途</th><th>家庭 / 用户</th><th>大小</th><th>校验</th><th>操作</th></tr></thead><tbody>{files.map(file=><tr className="border-t border-slate-200 dark:border-slate-700" key={file.id}><td className="p-3">{file.originalName}<br/><span className="text-muted">{file.mimeType} · {file.status}</span></td><td>{file.purpose}</td><td>{file.family.name}<br/>{file.owner.email}</td><td>{formatBytes(file.size)}</td><td className="font-mono text-xs">{file.sha256.slice(0,16)}…</td><td>{file.deletedAt?<span className="text-muted">已归档</span>:<form action={archiveUploadedFile}><input type="hidden" name="id" value={file.id}/><button className="button-ghost text-red-700">归档</button></form>}</td></tr>)}</tbody></table></div></div>}
