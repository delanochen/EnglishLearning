import { db } from "@/lib/db"; import { requireSystemAdmin } from "@/modules/authorization/require-admin"; import { formatBytes } from "@/modules/operations/status";
export default async function AdminFilesPage(){await requireSystemAdmin();const files=await db.uploadedFile.findMany({take:200,orderBy:{createdAt:"desc"},include:{owner:true,family:true}});return <div className="mx-auto max-w-6xl"><h1 className="text-4xl font-black">上传文件</h1><div className="card mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th className="p-3">文件</th><th>用途</th><th>家庭 / 用户</th><th>大小</th><th>校验</th></tr></thead><tbody>{files.map(file=><tr className="border-t border-slate-200 dark:border-slate-700" key={file.id}><td className="p-3">{file.originalName}<br/><span className="text-muted">{file.mimeType}</span></td><td>{file.purpose}</td><td>{file.family.name}<br/>{file.owner.email}</td><td>{formatBytes(file.size)}</td><td className="font-mono text-xs">{file.sha256.slice(0,16)}…</td></tr>)}</tbody></table></div></div>}
