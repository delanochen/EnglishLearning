import { auth } from "@/auth"; import { db } from "@/lib/db"; import { ProfilePicker } from "@/components/profile-picker"; import { SpeechPlayer } from "@/components/speech-player"; import { getAccessibleProfiles } from "@/modules/learner/access"; import { placementQuestions } from "@/modules/placement/questions"; import { submitPlacementTest } from "@/modules/placement/actions";
export default async function PlacementPage({ searchParams }: { searchParams: Promise<{ profile?: string; completed?: string }> }) { const session = await auth(); const query = await searchParams; const profiles = await getAccessibleProfiles(session!.user.id); const selected = profiles.find((profile) => profile.id === query.profile) ?? profiles[0]; const history = selected ? await db.placementTest.findMany({ where: { learnerProfileId: selected.id, status: "COMPLETED" }, include: { answers: true }, orderBy: { version: "desc" } }) : []; return <div className="mx-auto max-w-5xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-brand">Placement test</p><h1 className="mt-2 text-4xl font-black">英语水平测试</h1><p className="mt-2 text-muted">覆盖词汇、理解、听力、阅读、语法和写作；可以重新测试，历史结果会保留。</p><ProfilePicker profiles={profiles} selectedId={selected?.id} pathname="/placement"/>{query.completed && history[0] && <section className="card mt-6 border-brand"><p className="font-bold text-brand">测试已完成</p><h2 className="mt-2 text-3xl font-black">评估等级：{history[0].assessedLevel?.replace("_", "-")}</h2><p className="mt-3">优势：{history[0].strengths.join("、") || "正在建立"}</p><p className="mt-2">重点：{history[0].weakAreas.join("、") || "综合提升"}</p><p className="mt-2 text-muted">第一周计划已自动生成。</p></section>}{selected && <form action={submitPlacementTest} className="mt-6 space-y-5"><input type="hidden" name="profileId" value={selected.id}/>{placementQuestions.map((question, index) => <fieldset className="card" key={question.id}><legend className="text-lg font-black">{index + 1}. {question.section}</legend><p className="mt-2">{question.prompt}</p>{question.section === "听力" && question.passage && <div className="mt-4"><SpeechPlayer text={question.passage}/></div>}{question.section === "阅读" && question.passage && <blockquote className="mt-4 rounded-xl bg-slate-100 p-4 leading-7 dark:bg-slate-800">{question.passage}</blockquote>}{question.options ? <div className="mt-4 space-y-2">{question.options.map((option) => <label className="block rounded-xl border p-3" key={option}><input className="mr-2" type="radio" name={question.id} value={option} required/>{option}</label>)}</div> : <textarea className="input min-h-32" name={question.id} required={question.id === "writing-1"} placeholder={question.id === "speaking-1" ? "此项可留空，或输入语音识别结果" : "Write in English…"}/>}</fieldset>)}<button className="button-primary w-full">提交测试并生成第一周计划</button></form>} {history.length > 0 && <details className="card mt-6"><summary className="cursor-pointer text-xl font-black">历史测试（{history.length}）</summary><div className="mt-4 space-y-3">{history.map((test) => <div className="rounded-xl bg-slate-100 p-4 dark:bg-slate-800" key={test.id}><strong>第 {test.version} 次 · {test.assessedLevel?.replace("_", "-")}</strong><p className="text-sm text-muted">{test.submittedAt?.toLocaleString("zh-CN")} · 优势 {test.strengths.join("、") || "—"} · 薄弱 {test.weakAreas.join("、") || "—"}</p></div>)}</div></details>}</div>; }
