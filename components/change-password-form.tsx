"use client";
import { useActionState } from "react"; import { changePasswordAction } from "@/modules/identity/actions";
export function ChangePasswordForm(){const[state,action,pending]=useActionState(changePasswordAction,undefined);return <form action={action} className="card mt-6 max-w-xl space-y-4"><label><span className="label">当前密码</span><input className="input" type="password" name="currentPassword" autoComplete="current-password" required/></label><label><span className="label">新密码（至少 12 个字符）</span><input className="input" type="password" name="newPassword" autoComplete="new-password" required/></label><label><span className="label">再次输入新密码</span><input className="input" type="password" name="confirmation" autoComplete="new-password" required/></label>{state?.error&&<p className="text-red-700">{state.error}</p>}{state?.success&&<p className="text-green-700">{state.success}</p>}<button className="button-primary" disabled={pending}>{pending?"正在更新…":"修改密码"}</button></form>}
