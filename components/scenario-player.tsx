"use client";
import{useCallback,useEffect,useMemo,useRef,useState}from"react";type Dialogue={id:string;speaker:string;roleName:string;textEn:string;textZh:string|null;sequence:number;cameraCue:string|null};type SubtitleMode="en"|"zh"|"both"|"none";
export function ScenarioPlayer({dialogues}:{dialogues:Dialogue[]}){const ordered=useMemo(()=>[...dialogues].sort((a,b)=>a.sequence-b.sequence),[dialogues]);const[index,setIndex]=useState(0);const[subtitle,setSubtitle]=useState<SubtitleMode>("both");const[autoPlay,setAutoPlay]=useState(false);const[loop,setLoop]=useState(false);const loopRef=useRef(false);const[rate,setRate]=useState(.88);const[role,setRole]=useState("");const[dictation,setDictation]=useState("");const[checked,setChecked]=useState(false);const current=ordered[index];const roles=useMemo(()=>[...new Set(ordered.map(x=>x.roleName))],[ordered]);useEffect(()=>{loopRef.current=loop;if(!loop)window.speechSynthesis?.cancel()},[loop]);const speak=useCallback((item=current)=>{if(!item||!("speechSynthesis"in window))return;window.speechSynthesis.cancel();const play=()=>{const utterance=new SpeechSynthesisUtterance(item.textEn);utterance.lang="en-US";utterance.rate=rate;utterance.onend=()=>{if(loopRef.current)return window.setTimeout(play,350);if(autoPlay&&index<ordered.length-1)setIndex(v=>v+1)};window.speechSynthesis.speak(utterance)};play()},[autoPlay,current,index,ordered.length,rate]);useEffect(()=>{if(autoPlay&&current){const timer=window.setTimeout(()=>speak(current),100);return()=>window.clearTimeout(timer)}},[autoPlay,current,speak]);useEffect(()=>{setDictation("");setChecked(false)},[index]);useEffect(()=>()=>window.speechSynthesis?.cancel(),[]);if(!current)return <p>课程尚无台词。</p>;const hiddenForRole=role===current.roleName;const normalize=(v:string)=>v.toLowerCase().replace(/[^a-z0-9']/g,"");const correct=normalize(dictation)===normalize(current.textEn);return <div><div className="relative overflow-hidden rounded-3xl bg-gradient-to-br from-teal-950 to-slate-900 p-7 text-white"><p className="text-sm text-teal-200">镜头：{current.cameraCue??"对话场景"}</p><p className="mt-8 text-sm font-bold uppercase tracking-wider">{current.roleName} · {current.speaker}</p>{!hiddenForRole&&(subtitle==="en"||subtitle==="both")&&<p className="mt-3 text-2xl font-black leading-relaxed">{current.textEn}</p>}{hiddenForRole&&<p className="mt-3 rounded-xl border border-dashed border-teal-300 p-4 text-teal-100">轮到你扮演 {current.roleName}，请说出台词。</p>}{!hiddenForRole&&(subtitle==="zh"||subtitle==="both")&&<p className="mt-3 text-teal-100">{current.textZh}</p>}<div className="mt-8 h-2 overflow-hidden rounded-full bg-white/20"><div className="h-full bg-teal-300" style={{width:`${(index+1)/ordered.length*100}%`}}/></div></div><div className="mt-4 flex flex-wrap gap-2"><button type="button" className="button-primary" onClick={()=>speak()}>▶ 播放本句</button><button type="button" className="button-ghost" disabled={index===0} onClick={()=>setIndex(v=>Math.max(0,v-1))}>上一句</button><button type="button" className="button-ghost" disabled={index===ordered.length-1} onClick={()=>setIndex(v=>Math.min(ordered.length-1,v+1))}>下一句</button><select className="input w-auto" value={subtitle} onChange={e=>setSubtitle(e.target.value as SubtitleMode)}><option value="both">双语字幕</option><option value="en">英文字幕</option><option value="zh">中文字幕</option><option value="none">隐藏字幕</option></select><label className="button-ghost">速度 <select className="bg-transparent" value={rate} onChange={e=>setRate(Number(e.target.value))}><option value="0.7">0.7×</option><option value="0.88">0.88×</option><option value="1">1×</option><option value="1.2">1.2×</option></select></label><label className="button-ghost"><input className="mr-2" type="checkbox" checked={autoPlay} onChange={e=>setAutoPlay(e.target.checked)}/>连续</label><label className="button-ghost"><input className="mr-2" type="checkbox" checked={loop} onChange={e=>setLoop(e.target.checked)}/>单句循环</label><select className="input w-auto" value={role} onChange={e=>setRole(e.target.value)}><option value="">观看全部角色</option>{roles.map(x=><option key={x} value={x}>扮演 {x}</option>)}</select></div><details className="card mt-5"><summary className="cursor-pointer font-black">听写练习</summary><p className="mt-2 text-sm text-muted">先隐藏字幕并播放本句，然后输入听到的内容。</p><div className="mt-3 flex gap-2"><input className="input" value={dictation} onChange={e=>{setDictation(e.target.value);setChecked(false)}}/><button type="button" className="button-ghost" onClick={()=>setChecked(true)}>检查</button></div>{checked&&<p className={correct?"mt-2 text-green-700":"mt-2 text-amber-700"}>{correct?"听写正确！":`参考答案：${current.textEn}`}</p>}</details><div className="mt-5 grid gap-2 sm:grid-cols-2">{ordered.map((item,i)=><button type="button" className={i===index?"rounded-xl border border-brand bg-brand/10 p-3 text-left":"rounded-xl border p-3 text-left"} onClick={()=>setIndex(i)} key={item.id}><strong>{item.roleName}</strong><span className="ml-2 line-clamp-1 text-sm text-muted">{item.textEn}</span></button>)}</div></div>}
