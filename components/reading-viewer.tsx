"use client";
import { useMemo, useRef, useState, useTransition } from "react"; import { collectReadingWord, saveReadingProgress } from "@/modules/reading/actions";
type DictionaryEntry = { word: string; phonetic: string | null; definition: string; definitionEn: string };
export function ReadingViewer({ profileId, articleId, body, translation, targetVocabulary, targetGrammar, dictionary, initialPercent = 0, initialPosition = 0 }: { profileId: string; articleId: string; body: string; translation: string | null; targetVocabulary: string[]; targetGrammar: string[]; dictionary: DictionaryEntry[]; initialPercent?: number; initialPosition?: number }) { const [showZh, setShowZh] = useState(true); const [rate, setRate] = useState(0.9); const [selected, setSelected] = useState<DictionaryEntry | null>(null); const [position, setPosition] = useState(initialPosition); const [message, setMessage] = useState(""); const [pending, startTransition] = useTransition(); const started = useRef(Date.now()); const sentences = useMemo(() => body.match(/[^.!?]+[.!?]+|[^.!?]+$/g)?.map((value) => value.trim()).filter(Boolean) ?? [body], [body]); const targets = new Set(targetVocabulary.map((word) => word.toLowerCase())); const dictionaryMap = new Map(dictionary.map((item) => [item.word.toLowerCase(), item])); function speak(text: string) { window.speechSynthesis.cancel(); const utterance = new SpeechSynthesisUtterance(text); utterance.lang = "en-US"; utterance.rate = rate; window.speechSynthesis.speak(utterance); } function save() { const percent = Math.min(99, Math.max(initialPercent, Math.round((position + 1) / sentences.length * 100))); const seconds = Math.round((Date.now() - started.current) / 1000); startTransition(async () => { await saveReadingProgress({ profileId, articleId, progressPercent: percent, currentPosition: position, readingSeconds: seconds }); setMessage(`进度已保存：${percent}%`); started.current = Date.now(); }); } return <section className="card mt-7"><div className="flex flex-wrap items-center gap-2"><button className="button-primary" type="button" onClick={() => speak(sentences[position])}>▶ 朗读当前句</button><button className="button-ghost" type="button" onClick={() => setPosition((value) => Math.max(0, value - 1))}>上一句</button><button className="button-ghost" type="button" onClick={() => setPosition((value) => Math.min(sentences.length - 1, value + 1))}>下一句</button><label className="button-ghost">语速 <select className="bg-transparent" value={rate} onChange={(event) => setRate(Number(event.target.value))}><option value="0.65">慢</option><option value="0.9">正常</option><option value="1.1">快</option></select></label><button className="button-ghost" type="button" onClick={() => setShowZh((value) => !value)}>{showZh ? "隐藏中文" : "显示中文"}</button></div><div className="mt-6 space-y-4 text-lg leading-8">{sentences.map((sentence, sentenceIndex) => <p className={sentenceIndex === position ? "rounded-xl bg-brand/10 p-3" : "p-3"} key={`${sentenceIndex}-${sentence}`}>{sentence.split(/(\b[A-Za-z']+\b)/).map((token, tokenIndex) => { const lower = token.toLowerCase(); const entry = dictionaryMap.get(lower); return targets.has(lower) || entry ? <button type="button" className={targets.has(lower) ? "font-bold text-brand underline decoration-2" : "underline decoration-dotted"} onClick={() => { if (entry) setSelected(entry); else speak(token); }} key={`${tokenIndex}-${token}`}>{token}</button> : <span className={targetGrammar.some((grammar) => sentence.toLowerCase().includes(grammar.toLowerCase())) ? "decoration-amber-500 decoration-2" : ""} key={`${tokenIndex}-${token}`}>{token}</span>; })}</p>)}</div>{selected && <div className="mt-5 rounded-2xl border border-brand/30 bg-brand/5 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><strong className="text-xl">{selected.word}</strong><span className="ml-2 text-muted">{selected.phonetic}</span></div><div className="flex gap-2"><button type="button" className="button-ghost" onClick={() => speak(selected.word)}>播放发音</button><button type="button" className="button-primary" onClick={() => startTransition(async () => { await collectReadingWord({ profileId, word: selected.word }); setMessage(`${selected.word} 已收藏到单词复习`); })}>收藏单词</button></div></div><p className="mt-2">{selected.definitionEn}</p><p className="text-muted">{selected.definition}</p></div>}{showZh && translation && <details className="mt-5 rounded-2xl bg-slate-100 p-4 dark:bg-slate-800"><summary className="cursor-pointer font-bold">中英文对照</summary><p className="mt-3 whitespace-pre-wrap leading-7">{translation}</p></details>}<div className="mt-5 flex items-center gap-3"><button type="button" className="button-primary" disabled={pending} onClick={save}>保存阅读进度</button><span className="text-sm text-muted">第 {position + 1}/{sentences.length} 句 {message}</span></div></section>; }
