type ProfilePickerProps = {
  profiles: Array<{ id: string; name: string }>;
  selectedId?: string;
  pathname: string;
};

export async function ProfilePicker(props: ProfilePickerProps) {
  void props;
  return null;
}
