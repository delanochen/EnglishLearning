"use client";
import { useEffect, useState } from "react";
export function SpeechPlayer({ text }: { text: string }) { const [supported, setSupported] = useState(false); const [rate, setRate] = useState(0.9); useEffect(() => setSupported("speechSynthesis" in window), []); function play() { window.speechSynthesis.cancel(); const utterance = new SpeechSynthesisUtterance(text); utterance.lang = "en-US"; utterance.rate = rate; window.speechSynthesis.speak(utterance); } return <div className="flex flex-wrap items-center gap-3"><button type="button" className="button-primary" disabled={!supported} onClick={play}>{supported ? "播放英语音频" : "浏览器不支持朗读"}</button><label className="text-sm">速度 <select className="button-ghost ml-2" value={rate} onChange={(event) => setRate(Number(event.target.value))}><option value="0.65">慢速</option><option value="0.9">正常</option><option value="1.1">快速</option></select></label><button type="button" className="button-ghost" onClick={() => window.speechSynthesis?.cancel()}>停止</button></div>; }
