import { describe, expect, it } from "vitest"; import { updateStreak } from "@/modules/streaks/streak";
describe("study streak", () => { it("increments on the next day", () => expect(updateStreak(3, 5, new Date("2026-07-15"), new Date("2026-07-16"))).toMatchObject({ currentDays: 4, longestDays: 5, changed: true })); it("does not double count the same day", () => expect(updateStreak(3, 3, new Date("2026-07-16"), new Date("2026-07-16"))).toMatchObject({ currentDays: 3, changed: false })); it("resets after a gap", () => expect(updateStreak(8, 8, new Date("2026-07-10"), new Date("2026-07-16"))).toMatchObject({ currentDays: 1, longestDays: 8 })); });
