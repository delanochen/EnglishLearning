import{describe,expect,it}from"vitest";import{selectExactLevelItems}from"@/modules/content/level-selection";
describe("level-specific content selection",()=>{const items=[{id:"a1",level:"A1"},{id:"a2",level:"A2"},{id:"b1",level:"B1"}];it("shows only the learner's exact level",()=>expect(selectExactLevelItems(items,"A2",item=>item.level)).toEqual([{id:"a2",level:"A2"}]));it("never substitutes a neighboring level",()=>expect(selectExactLevelItems(items,"B2",item=>item.level)).toEqual([]));it("shows no graded content before placement",()=>expect(selectExactLevelItems(items,null,item=>item.level)).toEqual([]))});
