import { describe, expect, it } from "vitest"; import { scoreSpeaking } from "@/modules/speaking/scoring";
describe("speaking scoring", () => { it("scores matching keywords", () => { const result = scoreSpeaking("Could I have a glass of water", "Could I have a glass of water please", 4); expect(result.accuracy).toBe(1); expect(result.completeness).toBe(1); expect(result.speechRateWpm).toBeGreaterThan(0); }); it("reports missing words", () => { const result = scoreSpeaking("I would like coffee", "hello"); expect(result.accuracy).toBe(0); expect(result.missingWords).toContain("coffee"); }); });
