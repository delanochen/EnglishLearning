import { describe, expect, it } from "vitest"; import { createWeeklyPlan } from "@/modules/planning/planner"; import { summarizeActivities } from "@/modules/reports/metrics"; import { reportInsights, reportPeriod } from "@/modules/reports/period";
describe("planning and reports", () => { it("creates seven scheduled plan items", () => { const plan = createWeeklyPlan(30, ["语法"]); expect(plan).toHaveLength(7); expect(plan[2].target).toContain("语法"); }); it("aggregates completion and scores", () => { const result = summarizeActivities([{ module: "READING", durationSeconds: 600, score: 0.8 }, { module: "READING", durationSeconds: 300, score: 1 }], 4, 3); expect(result.durationMinutes).toBe(15); expect(result.averageScore).toBe(90); expect(result.completionRate).toBe(75); }); it("uses stable calendar periods for refreshable scheduled reports", () => { const now = new Date("2026-07-17T18:30:00Z"); expect(reportPeriod("WEEKLY", now)).toEqual({ start: new Date("2026-07-13T00:00:00Z"), end: new Date("2026-07-17T00:00:00Z") }); expect(reportPeriod("MONTHLY", now).start).toEqual(new Date("2026-07-01T00:00:00Z")); }); it("derives report guidance from current activity", () => { expect(reportInsights({ activityCount: 2, completionRate: 50, strongestModule: "READING" })).toEqual({ strengths: ["READING 参与度最高"], weakAreas: ["任务完成率需要提高"], recommendations: ["继续保持每日短时学习", "优先复习错题和到期单词"] }); }); });
