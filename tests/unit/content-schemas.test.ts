import { describe, expect, it } from "vitest"; import { dailyTaskOutputSchema, placementReportSchema, vocabularyCourseSchema } from "@/modules/ai/content-schemas";
describe("AI content schemas", () => { it("rejects incomplete vocabulary output", () => expect(vocabularyCourseSchema.safeParse({ word: "learn" }).success).toBe(false)); it("accepts a structured daily task", () => expect(dailyTaskOutputSchema.safeParse({ taskType: "READING", title: "Read", description: "One article", estimatedMinutes: 10, xpReward: 10, reason: "level" }).success).toBe(true)); it("bounds placement section scores", () => expect(placementReportSchema.safeParse({ assessedLevel: "A2", sectionScores: { reading: 120 }, strengths: [], weakAreas: [], recommendations: [] }).success).toBe(false)); });
